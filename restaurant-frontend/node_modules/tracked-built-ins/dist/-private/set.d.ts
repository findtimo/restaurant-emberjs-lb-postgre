declare class TrackedSet<T = unknown> implements Set<T> {
    private collection;
    private storages;
    private vals;
    private storageFor;
    private dirtyStorageFor;
    constructor();
    constructor(values: readonly T[] | null);
    constructor(iterable: Iterable<T>);
    // **** KEY GETTERS ****
    has(value: T): boolean;
    // **** ALL GETTERS ****
    entries(): IterableIterator<[T, T]>;
    keys(): IterableIterator<T>;
    values(): IterableIterator<T>;
    forEach(fn: (value1: T, value2: T, set: Set<T>) => void): void;
    get size(): number;
    [Symbol.iterator](): IterableIterator<T>;
    get [Symbol.toStringTag](): string;
    // **** KEY SETTERS ****
    add(value: T): this;
    delete(value: T): boolean;
    // **** ALL SETTERS ****
    clear(): void;
}
declare class TrackedWeakSet<T extends object = object> implements WeakSet<T> {
    private storages;
    private vals;
    private storageFor;
    private dirtyStorageFor;
    constructor(values?: readonly T[] | null);
    has(value: T): boolean;
    add(value: T): this;
    delete(value: T): boolean;
    get [Symbol.toStringTag](): string;
}
export { TrackedSet, TrackedWeakSet };
