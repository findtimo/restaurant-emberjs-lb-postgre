import { type WantedEngine } from '@pnpm/package-is-installable';
export declare function packageIsInstallable(pkgPath: string, pkg: {
    engines?: WantedEngine;
    cpu?: string[];
    os?: string[];
    libc?: string[];
}, opts: {
    engineStrict?: boolean;
    nodeVersion?: string;
}): void;
